const inputsPayment = document.querySelectorAll("input[name=payment]")

payment = JSON.parse(localStorage.getItem('payment')) || 0;

inputsPayment.forEach(input => {
    // input.addEventListener("click",() => {
    //     payment = input.value
    // })
    if(input)
})

